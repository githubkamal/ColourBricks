namespace ColourBricks.Domain;

/// <summary>
/// Type anchor for reflection-based tests that need the Domain assembly.
/// The Domain layer references nothing outside the BCL (plan.md §4).
/// </summary>
public sealed class AssemblyMarker;
